// Import { type Request, type Response } from "express";
// import { type WorkersRepository } from "../repository/types";

// class WorkersController {
//   constructor(public workers: WorkersRepository[]) {}

//   getWorks = async (_req: Request, res: Response): Promise<void> => {
//     const workers = await this.workers.getWorks();

//     res.status(200).json({ workers });
//   };
// }

// export default WorkersController;
